name: Ping Streamlit App

on:
  schedule:
    # Ejecutar todos los días a las 12:00 UTC (mañana en Colombia)
    - cron: '0 12 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping SPAE Standalone
        run: curl -sL https://spae-standalone-afywudedgwmqxj4qgb3qvn.streamlit.app/ > /dev/null
